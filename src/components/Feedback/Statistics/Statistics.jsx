import React from 'react';
import PropTypes from 'prop-types';
import css from components / Feedback / Statistics / statistics.module.css;

class Statistics extends React.Component {
    render() {
        return (

    );
    }
}
